class NewUser {
  constructor(name, age, dateOfBirth, password, gender, about) {
    this.name = name;
    this.age = age;
    this.dateOfBirth = dateOfBirth;
    this.password = password;
    this.gender = gender;
    this.about = about;
  }
}

export async function addNewUser(name, age, dateOfBirth, password, gender, about) {
    // post call
}

export async function loginDetails(
 
) {
  // get call
}

export async function updateUserData() {
  // put call
}

export async function deleteUserData() {
  // delete call
}

export async function getGenderDetails() {
  // get call
}